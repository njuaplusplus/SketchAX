pragma options "--bnd-intbits 9";
int W = 5;

bit[W] pbe_test92SK(bit[W] in) {
	bit[W*32] gT0 = ??;
	int t3 = (int) in ;
	bit[W] stepout = gT0[(W*t3)::W];
	return stepout;	
}

bit[W] test92(bit[W] in){	
	return in+in+in;	
}
