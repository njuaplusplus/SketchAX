int N = 3;
int SUM = 15;

int select(int[N] input){
    int i = 0;
    for (i = 0; i < N-1; i += 1) {
        if (input[i] + input[i+1] > SUM) return input[i]+input[i+1];
    }
    return 0;
}

int selectSK0 (int[N] input) implements select {
    repeat(??) {
        if (input[??] + input[??] > SUM) return input[??]+input[??];
    }
    return 0;
}

