int W=4;

int sum4Less(int[W] in, int v)
{
    int ret = 0;
    for(int i=0;i<W;i++){
        if(in[i] < v)
            ret = ret + in[i];
    }
    return ret;
}

int pbe_sum4LessSK0(int[W] in, int v) 
{
    int cnt=??;
    repeat(??)
    {
        // if ({| in[??] (== | != | > | >=) v | ??  |}) {
        if ({| in[??] (== | != | > | < ) v | ??  |}) {
            cnt += {| in[??] | ?? |};
        }
    }
    return cnt;
}


