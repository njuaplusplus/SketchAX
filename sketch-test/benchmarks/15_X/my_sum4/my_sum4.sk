int W=4;

int sum(int[W] in)
{
    int ret = 0;
    for(int i=0;i<W;i++){
        ret = ret + in[i];
    }
    return ret;
}

int sumSK0(int[W] in) implements sum
{
    int cnt=??;
    repeat(W)
    {
        cnt += {| in[??] | ?? |};
    }
    return cnt;
}

