int W=8;

bit[W] countBits(bit[W] in)
{
	bit[W] ret=0;
	bit[W] one = 0;
	one[0] = 1;
	for(int i=0;i<W;i++){
		if(in[i])
			ret=ret+one;
	}
	return ret;
}



bit[W] pbe_countSketch(bit[W] in) 
{
	bit[W] tmp=in;
	loop(??)
	{
		bit[W] m=??;
		tmp=(tmp & m) + (tmp>>?? & m);
	}
	return tmp;
}


